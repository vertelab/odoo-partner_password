# -*- coding: utf-8 -*-
from openerp.tests import common

class TestPartnerService(common.TransactionCase):
	def test_partner_service(self)
	env = self.env
	record = env['partner_service.0'].create({})
